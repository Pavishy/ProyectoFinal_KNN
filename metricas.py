def Manhatan(A,B):
    distancia = sum([abs(A[i] - B[i]) for i in range(len(A))])
    return distancia
def Euclidiana(A,B):
    distancia = sum([abs(A[i] - B[i]) for i in range(len(A))])
    return distancia
def Coseno(A,B):
    distancia = sum([abs(A[i] - B[i]) for i in range(len(A))])
    return distancia
def EuclidianaPromedio(A,B):
    distancia = sum([abs(A[i] - B[i]) for i in range(len(A))])
    return distancia
def DiferenciaDeCaracteresPromedio(A,B):
    distancia = sum([abs(A[i] - B[i]) for i in range(len(A))])
    return distancia
def Canberra(A,B):
    distancia = sum([abs(A[i] - B[i]) for i in range(len(A))])
    return distancia
def BrayCurtis(A,B):
    distancia = sum([abs(A[i] - B[i]) for i in range(len(A))])
    return distancia
def Orloci(A,B):
    distancia = sum([abs(A[i] - B[i]) for i in range(len(A))])
    return distancia
